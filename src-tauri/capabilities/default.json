{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "agtalk 默认权限：核心 API + 窗口关闭（审批弹窗提交后自动关闭）",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-close"]
}
