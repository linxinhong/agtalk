{
  "name": "agtalk",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "typecheck": "vue-tsc --noEmit",
    "build:extension": "cd extension && pnpm run build",
    "typecheck:extension": "cd extension && pnpm run typecheck"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-i18n": "^11.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vue-tsc": "^2.0.0"
  }
}
